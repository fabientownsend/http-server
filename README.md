## Run
``` bash
  gradle execute
```

By default the server run on the port 5000.
You can connect to the server with netcat this way:

``` bash
  nc localhost 5000
```

## Test
``` bash
  gradle test
```
